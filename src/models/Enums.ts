export enum  ReviewStateEnum {
    Open = 1,
    Closed
};

export enum RoleInReviewEnum {
    Author = 1,
    Reviewer,
    Watcher
};

export enum ParticipantStateEnum {
    Unread = 1,
    Read,
    Accepted,
    Rejected
};